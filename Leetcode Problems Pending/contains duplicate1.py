def contDuplicate(arr):
    found=False
    length=len(arr)
    if length==1 or length==0:
        return False
    else:
        '''lo=0
        hi=length-1
        while lo>=hi:'''
        sort.arr()
        for i in range(length-1): # stop until 2nd to the last element
            if arr[i]==arr[i+1]
                found=True
                break
        return found

def main():
    arr=[1,5,6,7,2,4,3,0,4,2]
